let isDone: boolean = false;

isDone = true;

console.log(typeof isDone); // 'boolean'

let isOk: boolean = true;

// let isNotOk: boolean = new Boolean(true); typescript에서는 이러한 방법보다는 위에  literal 방법으로 작성한다.
